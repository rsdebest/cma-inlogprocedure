Built using HTML5 boilerplate, HammerJS and Compass

Install Compass & Sass first:
"$ sudo gem install sass"
"$ sudo gem install compass"

Dont forget to run "$ compass watch /path/to/project" before editing scss files

